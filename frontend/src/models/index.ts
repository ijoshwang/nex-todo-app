export interface DutyDTO {
  id: string
  name: string
  created_at: string
  updated_at: string
  is_completed: boolean
}

export interface Duty {
  id: string
  name: string
  createdAt: string
  updatedAt: string
  isCompleted: boolean
}
